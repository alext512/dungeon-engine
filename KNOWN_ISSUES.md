# Known Issues / Bugs

## No currently confirmed open issues

The current sample project does not have any tracked engine bugs that still
need a fix.

Historical fixed bugs are better treated as change history than as active
issues, so those notes now live in `CHANGELOG.md` and the test suite instead of
this file.
