/*
  NOTES

  Set a world entity's logical grid position. This updates occupancy/grid state
  without snapping the entity's pixel position.
*/
{
  "inputs": {
    "entity_id": {
      "type": "entity_id"
    },
    "x": {
      "type": "int"
    },
    "y": {
      "type": "int"
    },
    "mode": {
      "type": "enum",
      "values": ["absolute", "relative"],
      "default": "absolute"
    },
    "persistent": {
      "type": "bool",
      "default": null
    }
  },
  "commands": [
    {
      "type": "set_entity_position",
      "entity_id": "$entity_id",
      "space": "world_grid",
      "x": "$x",
      "y": "$y",
      "mode": "$mode",
      "persistent": "$persistent"
    }
  ]
}
